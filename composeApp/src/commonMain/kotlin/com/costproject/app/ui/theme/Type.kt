package com.costproject.app.ui.theme

import androidx.compose.material3.Typography

/**
 * Typography for the app. Currently the Material 3 defaults; kept as its own
 * file so type changes have an obvious home, matching the reference project.
 */
val CostProjectTypography = Typography()
